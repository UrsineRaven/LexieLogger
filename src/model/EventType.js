class EventType {
  constructor(id, name, formatting, show) {
    this.id = id,
    this.name = name,
    this.formatting = formatting,
    this.show = show
  }
}

module.exports = {
  EventType: EventType
};
